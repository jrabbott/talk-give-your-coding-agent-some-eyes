{
  "name": "slides-as-code",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable Vite + reveal.js starter with a readability-first (Atkinson Hyperlegible) slide theme",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@fontsource/atkinson-hyperlegible": "^5.3.0",
    "reveal.js": "^6.0.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
